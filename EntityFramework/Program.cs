using DataBase_Generator;
using Microsoft.EntityFrameworkCore;
using System;
using System.Linq;

namespace EntityFramework
{
    public class Program
    {
        static void Main(string[] args)
        {
            //var context = new GeneratorContext();
            //var paymentsResult = context.Payments.FromSqlRaw("PaymentsFromXML").ToList();
            //var peopleResult = context.People.FromSqlRaw("PeopleFromXML").ToList();


            //var result = new EFService(context);
            //var LastMonthPayments = result.LastMonthPayments();
            //var MaxAveragePayment = result.MaxAveragePayment();
            //var UserSumPayments = result.UserSumPayments(id: 45);
            //var Top3MaxAndMin = result.Top3MaxAndMin();

            //foreach (var item in collection)
            //{
            //    Console.WriteLine($"{item.Name} {item.LastName} - {item.TotalSum}");
            //}
        }
    }
}
